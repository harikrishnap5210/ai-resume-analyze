services:
  - type: web
    name: resume-analyzer
    env: python
    rootDir: render_deploy
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn resumeAnalyzer_groq:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false

services:
  - type: web
    name: resume-analyzer
    env: python
    buildCommand: pip install -r render_deploy/requirements.txt
    startCommand: uvicorn render_deploy.resumeAnalyzer_groq:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
